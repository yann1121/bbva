//funcion pares
let array = [5,5,6,2,9,1,2,8,3,6];
let pares = [];
let j=1;
 for(let i= 0;i<array.length;i++){
    if(i % 2 ==0){
        impares.push(array[i]);
    }
    else{
        pares.push(array[i]);
    }
    
      for (j; j < array.length; j++) {
  
}
}

console.log("Los numeros pares son " + pares);